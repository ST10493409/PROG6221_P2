<Window x:Class="CyberSecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CyberSecurityChatbot"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10" Background="#FFE3FFD0">

        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <!-- CHAT AREA -->
        <ListBox x:Name="lstChat" Grid.Row="1" Margin="10" FontSize="16" Background="#2D2D30" Foreground="White"/>

        <!-- INPUT AREA -->
        <Grid Grid.Row="2" Margin="10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtName" Margin="10" FontSize="16" VerticalContentAlignment="Center" Text="Enter your name" Background="#FFABFFF9" FontWeight="Bold" TextAlignment="Center"/>

            <TextBox x:Name="txtMessage" Grid.Column="1" Margin="10" FontSize="16" VerticalContentAlignment="Center" Text="Type your message" Background="#FFAAEDFF" TextAlignment="Center" FontWeight="Bold"/>

            <Button x:Name="btnSend" Grid.Column="2" Margin="10" FontSize="20" Content="Send" Background="#007ACC" Foreground="White" Click="btnSend_Click" FontWeight="Bold"/>

        </Grid>

    </Grid>

</Window>
